spring.application.name=perfumeria

# Puerto dinámico de Render
server.port=${PORT:8080}

# Base de Datos Supabase (PostgreSQL) con SSL Obligatorio
spring.datasource.url=jdbc:postgresql://db.mshlpehrumsqvucpjgtf.supabase.co:6543/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate (Dialecto eliminado para autodetección)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format.sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Tiempos de espera optimizados para conexiones en la nube
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# Seguridad
app.security.jwt-secret=${JWT_SECRET:velvet-essence-development-secret-key-2026-strong}
app.security.seed-admin=${SEED_ADMIN:true}
app.security.admin-email=${ADMIN_EMAIL:admin@velvet.com}
app.security.admin-password=${ADMIN_PASSWORD:Admin123!}
